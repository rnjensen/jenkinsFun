pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo 'Compile'
            }
        }
        
        stage('test') {
            steps {
                echo 'Test'
            }
        }
        
        stage('deploy') {
            steps {
                echo 'Deploy'
            }
        }
    }
}
